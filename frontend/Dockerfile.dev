FROM node:20-alpine

WORKDIR /app

# Install dependencies (will be done when package.json is ready)
COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

# Dev server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
